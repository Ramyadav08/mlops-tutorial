# __init__.py file is specifically used to mark a directory as a Python package.
# It can also be used to execute package initialization code or set the __all__ variable.
# In this case, we are leaving it empty to indicate that this directory is a package.